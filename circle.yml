machine:
  services:
    - docker

  # Version of ruby to use
  ruby:
    version:
     2.1.5
dependencies:
  bundler:
    without: [  ]

test:
  override:
    - bundle exec rake test

deployment:
  release:
    branch: master 
    commands:
      - git config user.name "CircleCi"
      - git config user.email "$CIRCLE_USERNAME@users.noreply.github.com"
      - curl --silent https://gist.githubusercontent.com/peterabbott/f654490db7d46fb0e73d/raw/97ad591316d6ddf4ee9b61abe28b9b320410089c/gemrelease.sh | bash 
